<div class="container">
 <h4>会員情報編集</h4>
    <%= form_with model: @user, url: user_path(current_user.id), local:true do |f| %>
    <table class="table table-borderless mt-3">
      <tr>
        <td>
          会員ID
        </td>
        <td>
          <%= @user.id %>
        </td>
      </tr>

      <tr>
        <td>
          名前
        </td>
        <td>
           <%= f.text_field :name %>
<!--メソッドは2種類ある-->
<!--インスタンスメソッドとクラスメソッドがある-->
<!--インスタンスメソッドをこちらは実行されている。.nameでインスタンス変数のnameカラムが出力されるなど-->
<!--扱うデータによってどちらで実行されるか決まる。-->
<!--変数.nameの変数の違い（文字が異なるではない）によって左右される-->
<!--データの取得の結果によって決まる。-->
<!--@userはusersテーブルの中の1レコード分のデータ-->
<!--searchのviewの@usersはusersテーブルの中にあるある条件に合致するすべてのデータを参照する-->
<!--これにより変化する。1行（レコード）or条件に合致するすべての行を取得するか-->
<!--ある1行のデータに対して行われるメソッドがインスタンスメソッド-->
<!--条件に合致する全ての行に対して行われるメソッドがクラスメソッド-->
<!--どのデータのカラムを参照するかが明確な場合はインスタンスメソッド(1行のデータを取得しているため明確)-->

<!--例-->
<!--users = User-->
<!--users.age-->
<!--NomethodErrorが起きる。ageというクラスメソッドが実行されようとするがそんなものはないためエラーが起きる-->

<!--users.name-->
<!--users.nameはインスタンス変数での.nameというメソッドもあるがusersが条件に合致するすべての行を取得しようとしていた（実際複数かどうかは関係ない）-->
<!--データなのでクラスメソッドが実行される。その結果nameカラムが表示されず、user.rbのclass名が表示-->
        </td>
      </tr>

      <tr>
        <td>
          メールアドレス
        </td>
        <td>
           <%= f.text_field :email %>
        </td>
      </tr>

      <tr>
        <td>
          <%= f.submit '編集内容を保存', class: "mt-3 btn btn-success" %>
        </td>
      </tr>
    </table>
    <% end %>
</div>

<!--ボタンを押してもエラーも出ない場合は，そもそも機能していない(HTMLの構造がおかしい)ので検証ツールを用いて確認する-->
<!--formの中にinputがあるかなどを確認する。-->
<!--tableタグとformは相性が悪い。tableタグはほかの要素と共存できない（入るデータが決まっている(tdなど)）ことがあるため注意-->
<!--tableタグの中にformがあると不具合が起きるのでtableの外に配置する．文法は間違っていないので注意．-->