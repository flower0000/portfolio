<div class="container">
  <div class="row"  style="width:350px;">
    <table class="table table-bordered mt-3">
      <tr>
        <td>
          会員ID
        </td>
        <td>
          <%= @user.id %>
        </td>
      </tr>

      <tr>
        <td>
          名前
        </td>
        <td>
          <%= @user.name %>
        </td>
      </tr>
    </table>
  </div>

  <div class="row">
    <% @recipes.each do |recipe| %>
      <div class="col-lg-4">
        <%= link_to recipe_path(recipe.id) do %>
          <%= attachment_image_tag recipe, :dish_photo, :fill, 300, 300, format: 'jpeg', fallback: "no_image.jpg", size:'300x300' , class: "mr-3 mb-3"%>
        <% end %>
      </div>
    <% end %>
  </div>
</div>